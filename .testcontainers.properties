# Docker Desktop on Windows/WSL2: use TCP so Testcontainers can connect.
# In Docker Desktop: Settings -> General -> "Expose daemon on tcp://localhost:2375 without TLS"
docker.host=tcp://localhost:2375
docker.tls.verify=0
